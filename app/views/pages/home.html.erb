<div class="p-5 mb-4 border rounded-3">
    <div class="container-fluid py-5">
        <h1 class="display-5 fw-bold">
            <%= user_signed_in? ? "Wellome to our website #{current_user.email}" : 'Wellcome to our website' %>
        </h1>
        <p class="col-md-8 fs-4">Nice to see you!</p>
        <% if user_signed_in? %>
            <%= link_to 'Go to explorer', videos_path, class:"btn btn-custom" %>
        <% else %>
            <%= link_to 'Sign in', new_user_session_path, class:"btn btn-custom" %>
        <% end %>
    </div>
</div>

<div class="row align-items-md-stretch">
    <div class="col-md-6">
        <div class="h-100 p-5 text-bg-dark rounded-3">
        <h2>Change the background</h2>
        <p>Swap the background-color utility and add a `.text-*` color utility to mix up the jumbotron look. Then, mix and match with additional component themes and more.</p>
        <button class="btn btn-outline-light" type="button">Example button</button>
        </div>
    </div>
    <div class="col-md-6">
        <div class="h-100 p-5 bg-light text-dark border rounded-3">
        <span>Add borders</span>
        <p>Or, keep it light and add a border for some added definition to the boundaries of your content. Be sure to look under the hood at the source HTML here as we've adjusted the alignment and sizing of both column's content for equal-height.</p>
        <button class="btn btn-secondary" type="button">Example button</button>
        </div>
    </div>
</div>
